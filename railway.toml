[build]
builder = "NIXPACKS"
buildCommand = "cd src/client && npm install && npm run build && cd ../.. && npm install"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10 